# Required: Google Gemini API key for AI triage and draft replies
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: host port for the frontend (default 3000)
FRONTEND_PORT=3000

# Optional: set to true to re-seed the database on container start
FORCE_SEED=false
